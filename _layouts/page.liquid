---
layout: default
---
{% if page._styles %}
  <!-- Page/Post style -->
  <style type="text/css">
    {{ page._styles }}
  </style>
{% endif %}

<style>
body .container {
  max-width: 1200px !important;
}
.post {
  max-width: 1200px !important;
  width: 100% !important;
  margin: 0 auto;
}
.post article {
  max-width: none !important;
  width: 100% !important;
}
.publications {
  max-width: none !important;
  width: 100% !important;
}
.container, .container-fluid {
  max-width: 1200px !important;
}
.bibliography {
  max-width: none !important;
  width: 100% !important;
}
</style>

<div class="post" style="max-width: 1200px; margin: 0 auto;">
  <header class="post-header">
    <h1 class="post-title">{{ page.title }}</h1>
    <p class="post-description">{{ page.description }}</p>
  </header>

  <article>
    {{ content }}
  </article>

  {% if page.related_publications %}
    <h2>References</h2>
    <div class="publications">
      {% bibliography --cited_in_order %}
    </div>
  {% endif %}

  {% if site.giscus and page.giscus_comments %}
    {% include giscus.liquid %}
  {% endif %}
</div>
